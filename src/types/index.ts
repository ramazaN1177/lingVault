export type IUser = {
    id: string;
    fullname: string;
    email: string;
    imageUrl: string;
  };
  
  export type INewUser = {
    fullname: string;
    email: string;
    password: string;
  };